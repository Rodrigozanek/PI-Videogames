import React from "react";
import Estilos from "../form/form.module.css"

function Form () {
    return (
        <div>
            <h1 className={Estilos.Titulo}>estamos en el Form</h1>
        </div>
    )
};

export default Form;